select
created,
updated,
deleted,
billing_request_id,
hubspot_deal_id,
cancellation_reason_id,
uuid,
reorder_original_order_uuid,
reorder_with_same_mp_order_uuid,
user_id,
quote_uuid,
status,
delivered_at,
shipped_at,
expected_shipping_date,
legacy_order_id,
support_ticket_id,
completed_at,
number,
in_production_at,
promised_shipping_date,
accepted_at,
is_eligible_for_restriction,
is_external,
order_change_request_status,
order_change_request_freshdesk_ticket_id
from {{ ref('network_services', 'gold_orders') }}
